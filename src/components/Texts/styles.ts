import styled from "styled-components";

export const PagePrimaryTitle = styled.h1`
  font-size: 28px;
  font-weight: bold;
  color: var(--color-title-one);
  text-align: center;
  margin-bottom: 8px;
`;

export const PageSecondaryTitle = styled.h2`
  font-size: 16px;
  font-weight: normal;
  color: var(--color-title-two);
  text-align: center;
`;

export const ItemPrimaryTitle = styled.h3`
  font-size: 18px;
  font-weight: 600;
  color: var(--color-title-one);
`;

export const ItemSecondaryTitle = styled.h4`
  font-size: 14px;
  font-weight: normal;
  color: var(--color-title-two);
`;

export const PrimaryText = styled.p`
  font-size: 22px;
  font-weight: lighter;
  color: var(--color-text);
`;

export const SecondaryText = styled.p`
  font-size: 12px;
  font-weight: lighter;
  color: var(--color-text);
`;

export const TertiaryText = styled.p`
  font-size: 14px;
  font-weight: lighter;
  color: var(--color-text);
`;
